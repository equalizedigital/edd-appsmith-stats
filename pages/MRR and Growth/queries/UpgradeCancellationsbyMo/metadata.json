{
  "gitSyncId": "63e8d02d1e242279f5911ffe_67dc626d295431490fd8c104",
  "id": "MRR and Growth_UpgradeCancellationsbyMo",
  "pluginId": "mysql-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n  m.month,\n  COUNT(DISTINCT CASE WHEN n.id IS NOT NULL THEN s.id END) AS subscriptions,\n  COALESCE(\n    ROUND(\n      SUM(\n        CASE\n          WHEN n.id IS NOT NULL THEN\n            CASE\n              WHEN s.created >= DATE_SUB(s.created, INTERVAL 12 MONTH) THEN s.initial_amount / 12\n              ELSE s.recurring_amount / 12\n            END\n          ELSE 0\n        END\n      ),\n      2\n    ),\n    0\n  ) AS mrr\nFROM (\n  SELECT DATE_FORMAT(DATE_SUB(NOW(), INTERVAL n MONTH), '%Y-%m') AS month\n  FROM (\n    SELECT 0 n UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4\n    UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9\n    UNION SELECT 10 UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14\n    UNION SELECT 15 UNION SELECT 16 UNION SELECT 17 UNION SELECT 18 UNION SELECT 19\n    UNION SELECT 20 UNION SELECT 21 UNION SELECT 22 UNION SELECT 23\n  ) numbers\n) m\nLEFT JOIN wp_edd_subscriptions s\n  ON DATE_FORMAT(s.expiration, '%Y-%m') = m.month\n  AND s.expiration > DATE_SUB(DATE_ADD(LAST_DAY(NOW()), INTERVAL 1 DAY), INTERVAL 2 YEAR)\n  AND s.expiration < NOW()\n  AND s.status = 'cancelled'\nLEFT JOIN wp_edd_notes n\n  ON n.object_id = s.id\n  AND n.object_type = 'subscription'\n  AND n.content LIKE '%cancelled for license upgrade%'\nGROUP BY m.month;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Easy Digital Downloads",
      "isAutoGenerated": false,
      "name": "Easy Digital Downloads",
      "pluginId": "mysql-plugin"
    },
    "dynamicBindingPathList": [],
    "name": "UpgradeCancellationsbyMo",
    "pageId": "MRR and Growth",
    "runBehaviour": "ON_PAGE_LOAD",
    "userSetOnLoad": false
  }
}